# Variáveis de ambiente para o serviço POSTGRESQL
# Inserir as credenciais no seu .env local para conectar ao banco de dados
# ANTES de rodar o Docker Compose

DB_DATABASE=postgres
DB_USERNAME=postgres
DB_PASSWORD=password

FRONTEND_PORT=3000
BACKEND_PORT=8000