# Frontend (Nuxt 4)

Aplicação SPA em Nuxt 4 com Vue 3 e TypeScript.

## Estrutura usada no projeto

- `app/Pages`
- `app/Layouts`
- `app/Composables`
- `app/Plugins`
- `app/components`

## Comandos

No fluxo padrão do monorepo, use o Makefile:

```bash
make up
make npm-install
make npm-build
```
